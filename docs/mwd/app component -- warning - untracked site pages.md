^ [[app component]] _link not tracked_ for [[site page tracking state -- untracked]]

---

Shows up in the [[app page -- site editor]] above the [[app component -- site page views]] and also in the [[app component - modal -- site preview]] (because it's so important).

It alerts the [[publisher]] to this [[publish mode -- dirty]] state.