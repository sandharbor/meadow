This is bootstrap config... not [[app config]] and not [[site config]] or [[site page config]]

It lives in `~/.config/meadow/bootstrap_config.yaml` if needed

It is config that needs to live in a known location, like [[bootstrap config -- meadowHomeDirectoryOverride]], which can describe where the _rest_ of the config lives.