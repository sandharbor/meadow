[[filter]]s alter _link not tracked_ behavior.
:
If a [[site page]] is caught up in a [[filter]], then by default it may not be impacted by certain _link not tracked_s
:
[[example - site pages impacted by regex filter marking the sensitive are not bulk-added by track to whitelist tooling]]