These are automatically-generated [[bundle page]]s.  They are not shown in the graph.

This is how you identify them:
* the [[bundle page config -- sourceGraphSubdirectory]] is `x-tagpages`
* the [[bundle page config -- title]] starts with `tag--`

They are added to support a tag-like interface in the [[rendered HTML]]