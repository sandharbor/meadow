In a [[source page]], the keys/values in the property block at the top of the page (also called frontmatter) that looks like this:

```
---
meadow-sensitive: true
---
```

One example is [[source page property -- meadow-sensitive]] (as shown above)

