### Either the default

[[meadow home directory default path - HOME slash Library slash Application Support slash Meadow slash MeadowHome]]

### Or the override

if the user sets one at [[bootstrap config -- meadowHomeDirectoryOverride]]