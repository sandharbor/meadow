^ [[app component]] _link not tracked_ for [[bundle page tracking state -- untracked]]

---

Shows up in the [[app page -- bundle editor]] above the [[app component -- bundle page views]] and also in the [[app component - modal -- bundle preview]] (because it's so important).

It alerts the [[publisher]] to this [[publish mode -- dirty]] state.