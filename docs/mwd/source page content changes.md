[[source page]] content changes

We can see when this happens in two places.  First we'll see the differences in [[site config content -- directory -- tracked_page_content]], which is versioned by git.  Second, the [[publisher]] can see the _impact_ of those changes by seeing the changes in the rendered HTML at the [[app component - modal -- site preview -- tab changes]].