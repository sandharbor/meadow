[[html generation]] benefits from [[plugin support]] because we want to support lots of different ways of organizing files and also lots of _link not tracked_s that maybe we didn't even think of initially.  For example, how do we export _link not tracked_

We also have [[hooks config]] which lets you make additional modifications to the files being generated.