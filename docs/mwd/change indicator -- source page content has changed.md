[[meadow change management]] for when [[source page content changes]]

This would involve leveraging the [[bundle config content -- directory -- tracked_page_content]]

But also, perhaps it is less important because of [[app component - modal -- bundle preview -- tab changes]]?  Perhaps we could add a "Markdown" tab along with the HTML tab, and we'd be able to see just the markdown changes?  That could be interesting.