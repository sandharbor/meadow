There are [[rust]] utilities for speed and several _link not tracked_ applications.

In rust and TypeScript we do our own [[project - git]] utilities because we don't want to rely on the [[publisher]]'s computer to have it installed already.