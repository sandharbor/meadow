[[filter page selector scope]] at [[scope -- global]]

A [[filter page selector]] scope that applies across all projects in the system. Global filters can be:
- Configured to apply (or not apply) to new [[site]]s
- Used in conjunction with [[multi site management]]
- toggled off for specific [[site]]s without needing to be deleted ( [[disabling global configuration at a per-site scope]] )
