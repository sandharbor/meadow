These are automatically-generated [[site page]]s.  They are not shown in the graph.

This is how you identify them:
* the [[site page config -- sourceGraphSubdirectory]] is `x-tagpages`
* the [[site page config -- title]] starts with `tag--`

They are added to support a tag-like interface in the [[rendered HTML]]