[package]
name = "link_parser"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
# No external dependencies needed for this logic

[lib]
name = "link_parser_lib"
path = "src/lib.rs" 