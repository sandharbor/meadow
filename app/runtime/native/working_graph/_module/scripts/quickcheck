#!/bin/bash
set -euo pipefail
cd "$(dirname "$0")/../../working_graph_code"
# Cargo decides whether rebuilding is necessary, including local Linkrange edits.
./make_release
cargo fmt --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked
