{
  "name": "meadow-runtime-supervisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/supervisorCli.ts --bundle --platform=node --format=cjs --target=node24 --outfile=dist/meadow-runtime-supervisor.cjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.27.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.7"
  }
}
