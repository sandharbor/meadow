id: disconnect-reference
label: Disconnect the reference source
action: "Move `reference://` out of its registered filesystem location to `reference-disconnected`."
check: "In Manage sources, remove disconnected `reference://` and Save. Review shows only the removed source and its affected pages; accepted material stays intact until acceptance."
e2e: multi-source-disconnect.spec.ts
categories:
  - move
  - remove
operations:
  - move:
      from: reference
      to: reference-disconnected
sourceGraph: multi-source
