id: relocate-research
label: Relocate the research source
action: "Move the entire `research://` directory to the filesystem location `research-relocated`."
check: "Accepted pages remain available; use Manage sources to reconnect `research://` at the new location."
e2e: multi-source-relocation.spec.ts
categories:
  - move
operations:
  - move:
      from: research
      to: research-relocated
sourceGraph: multi-source
