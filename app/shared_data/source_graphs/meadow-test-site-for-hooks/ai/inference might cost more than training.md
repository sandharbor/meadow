[[inference]] might cost more than [[training]].  The reason is that inference happens potentially for a very long time. Training, once completed finishes. The hosting costs of inference can get astronomical. [[diff - training vs inference]]

Particularly true for [[reasoning model]]s using lots of [[test-time compute]] like [[model - o3]]